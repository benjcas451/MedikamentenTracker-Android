<resources>
    <!-- Nur die Hülle um den Compose-Inhalt: dunkler Grund nach Design-Guide,
         damit beim App-Start kein andersfarbiger Frame aufblitzt. -->
    <style name="MedikamenteWearTheme" parent="@android:style/Theme.DeviceDefault">
        <item name="android:windowBackground">@color/fenster_hintergrund</item>
    </style>
</resources>
